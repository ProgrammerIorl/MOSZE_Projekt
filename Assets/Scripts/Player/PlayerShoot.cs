using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerShoot : MonoBehaviour
{
    
    /*public WeaponScriptableObject scriptableObject;
    float lastfired;
    
    private void Update()
    {
        
        if (Time.time - lastfired > 1 / scriptableObject.fireRate)
        {
            lastfired = Time.time;
            GameObject clone = Instantiate(scriptableObject.projectile, transform.position, transform.rotation);
            Rigidbody2D rb = clone.GetComponent<Rigidbody2D>();
            rb.velocity = transform.TransformDirection(Vector3.up *scriptableObject.projectileSpeed);
        }
    }*/
    // Start is called before the first frame update
    
}
